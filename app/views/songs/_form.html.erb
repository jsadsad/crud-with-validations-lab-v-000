<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2>There were some errors:</h2>
      <ul>
        <% @song.errors.full_message.each do |message| %>
        <li><%= message %></li>
      </ul>

    </div>
  <% end %>
<%= end %>
